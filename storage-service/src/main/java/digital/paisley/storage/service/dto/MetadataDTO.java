package digital.paisley.storage.service.dto;

/**
 * Base interface for metadata DTOs.
 */
public interface MetadataDTO {
    // Base interface can be extended to include common metadata fields if needed in the future.
}
